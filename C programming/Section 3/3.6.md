while and for loops test the condition at the "top" of the loop, before passing through the loop body

alternatively, the do-while loop ensures the loop body is executed at least once before terminating

the syntax is

```
do
	statement;
while (expression);
```
